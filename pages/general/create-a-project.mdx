---
name: Create a project
---

# Create a Project

to initalize a c3pm project you can do:

```shell
$ ctpm init
```

you will then have this architecture:

```
project-name
│
└── .c3pm
│     └── cmake
│          └── CMakeLists.txt
└── c3pm.yml
│
└── src
    └── main.cpp
```
